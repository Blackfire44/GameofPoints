<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="android:style/Theme.NoTitleBar.Fullscreen">
        <!-- Customize your theme here. -->
        <item name="android:textSize" >@dimen/text_size</item>
    </style>

    <style name="CustomDialogTheme" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="colorAccent">@color/black</item><!--Buttons color-->

        <item name="android:textColorPrimary">@color/black</item><!--title and message color-->

        <item name="android:windowBackground">@drawable/background_dialog</item> <!--dialog background-->
    </style>

</resources>